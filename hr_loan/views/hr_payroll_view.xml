<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="act_employee_loan" model="ir.actions.act_window">
            <field name="name">Employee Loan Details</field>
            <field name="res_model">hr.loan.line</field>
            <field name="view_mode">list,form</field> <field name="context">{
                'default_payslip_id': active_id,
                'search_default_payslip_id': active_id,
                'create': 0,
                'edit': 0
            }</field>
            <field name="domain">[('payslip_id','=',active_id)]</field>
        </record>

        <record id="hr_payslip_form_inherit_view" model="ir.ui.view">
            <field name="name">hr.payslip.inherit.form</field>
            <field name="model">hr.payslip</field>
            <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="hr_loan.act_employee_loan" 
                            class="oe_stat_button"
                            icon="fa-money"
                            type="action">
                        <field name="total_amount_paid" widget="statinfo" string="Loans"/>
                    </button>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
