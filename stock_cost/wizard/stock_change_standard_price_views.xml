<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_change_standard_price" model="ir.ui.view">
            <field name="name">Change Standard Price</field>
            <field name="model">stock.change.standard.price</field>
            <field name="arch" type="xml">
                <form string="Change Standard Price">
                    <separator string="Change Price"/>
                    <field name="counterpart_account_id_required" invisible="1"/>
                    <group>
                        <group>
                            <field name="new_price" string="Cost"/>
                        </group>
                        <!-- <group>
                            <field name="counterpart_account_id" options="{'no_create': True}"
                                attrs="{'invisible': [('counterpart_account_id_required', '=', False)], 'required': [('counterpart_account_id_required', '=', True)]}"/>
                        </group> -->
                    </group>
                    <footer>
                        <button name="change_price" string="_Apply" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_view_change_standard_price" model="ir.actions.act_window">
            <field name="name">Change Standard Price</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">stock.change.standard.price</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_change_standard_price"/>
            <field name="target">new</field>
        </record>


        <record model="ir.ui.view" id="product_form_view_inherit_update_cost">
            <field name="name">product.product.cost</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_normal_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//form/header" position="inside">
                    <field name="cost_method" invisible="1"/>
                    <button string="Update Foreign Cost" type="action"
                            name="%(stock_cost.action_view_change_standard_price)d"
                            groups="stock.group_stock_user"
                            invisible="cost_method == 'fifo'"
                    />
                </xpath>
                <xpath expr="//button[@name='%(stock_cost.action_view_change_standard_price)d']" position="attributes">
                    <attribute name="groups">stock.group_stock_user</attribute>
                </xpath>

            </field>
        </record>

        <record model="ir.ui.view" id="product_form_view_inherit_update_cost2">
            <field name="name">product.product.cost</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <header position="inside">
                    <field name="cost_method" invisible="1"/>
                    <button string="Update Foreign Cost" type="action"
                            groups="stock.group_stock_user"
                            name="%(stock_cost.action_view_change_standard_price)d"
                            invisible="(cost_method == 'fifo') or (product_variant_count > 1)"
                    />
                </header>
            </field>
        </record>

        <record id="action_update_cost" model="ir.actions.server">
            <field name="name">Update Foreign Cost</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="binding_model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">records.change_price()</field>
        </record>


    </data>
</odoo>
