<odoo>
    <data>

        <record id="view_category_property_form" model="ir.ui.view">
            <field name="name">product.category.stock.property.form.inherit</field>
            <field name="model">product.category</field>
            <field name="inherit_id" ref="stock_account.view_category_property_form" />
            <field name="arch" type="xml">
                <field name="property_stock_valuation_account_id" position="after">
                    <field name="property_stock_account_currency_adjustment"/>
                </field>
            </field>
        </record>

        <record id="product_template_only_form_view_inherit_id" model="ir.ui.view">
            <field name="name">product.template.product.form</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_form_view"/>
            <field name="arch" type="xml">
                <!-- Load force_currency_id and valuation as invisible so they can
                     be used by currency_field option and readonly expression. -->
                <xpath expr="//field[@name='cost_currency_id']" position="after">
                    <field name="force_currency_id" invisible="1"/>
                    <field name="valuation" invisible="1"/>
                </xpath>
                <!-- Restrict standard_price (label + field div) to group_product_cost -->
                <xpath expr="//label[@id='standard_price_label']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <xpath expr="//div[@name='standard_price_uom']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <!-- Foreign Cost: displayed directly below the Cost (standard_price) row -->
                <xpath expr="//div[@name='standard_price_uom']" position="after">
                    <label for="foreign_standard_price" string="Foreign Cost"
                           invisible="(id and product_variant_count != 1 and not is_product_variant) or type == 'combo'"/>
                    <div name="foreign_standard_price_uom"
                         invisible="(id and product_variant_count != 1 and not is_product_variant) or type == 'combo'">
                        <field name="foreign_standard_price"
                               class="oe_inline"
                               widget="monetary"
                               options="{'currency_field': 'force_currency_id', 'field_digits': True}"
                               readonly="valuation == 'real_time'"/>
                    </div>
                </xpath>
            </field>
        </record>

        <record id="product_template_only_form_view_inherit_id_product" model="ir.ui.view">
            <field name="name">product.prod.product.form</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_normal_form_view"/>
            <field name="arch" type="xml">
                <!-- Load force_currency_id and valuation as invisible so they can
                     be used by currency_field option and readonly expression. -->
                <xpath expr="//field[@name='categ_id']" position="after">
                    <field name="valuation" invisible="1"/>
                    <field name="force_currency_id" invisible="1"/>
                </xpath>
                <!-- Restrict standard_price (label + field div) to group_product_cost -->
                <xpath expr="//label[@id='standard_price_label']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <xpath expr="//div[@name='standard_price_uom']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <xpath expr="//field[@name='standard_price']" position="attributes">
                    <attribute name="readonly">[('valuation','=','real_time')]</attribute>
                </xpath>
                <!-- Foreign Cost: displayed directly below the standard cost -->
                <xpath expr="//div[@name='standard_price_uom']" position="after">
                    <label for="foreign_standard_price" string="Foreign Cost"
                           invisible="(id and product_variant_count != 1 and not is_product_variant) or type == 'combo'"/>
                    <div name="foreign_standard_price_uom"
                         invisible="(id and product_variant_count != 1 and not is_product_variant) or type == 'combo'">
                        <field name="foreign_standard_price"
                               class="oe_inline"
                               widget="monetary"
                               options="{'currency_field': 'force_currency_id', 'field_digits': True}"
                               readonly="valuation == 'real_time'"/>
                    </div>
                </xpath>
            </field>
        </record>


        <record model="ir.ui.view" id="view_stock_quant_list_inherit2">
            <field name="name">stock.quant.list</field>
            <field name="model">stock.quant</field>
            <field name="inherit_id" ref="stock.view_stock_quant_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='quantity']" position="before">
                    <field name="force_currency_id" groups="stock.group_stock_manager"/>
                </xpath>
            </field>
        </record>


        <!--Price List -->
        <record id="inherit_product_pricelist_item_form_view" model="ir.ui.view">
            <field name="name">product.pricelist.item.form</field>
            <field name="model">product.pricelist.item</field>
            <field name="inherit_id" ref="product.product_pricelist_item_form_view"/>
            <field name="arch" type="xml">
                <field name="base" position="replace">
                    <field name="base" colspan="6"/>
                </field>
                <xpath expr="//group[@name='pricelist_rule_base']//group[1]" position="inside">
                    <div class="o_row" invisible="base != 'foreign_standard_price'" style="width: 40% !important;">
                    <span>Forgen Cost -</span>
                </div>
                </xpath>
            </field>
        </record>

        <record id="product_variant_easy_edit_view_inherit_id_product" model="ir.ui.view">
            <field name="name">product.prod.product.form</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_variant_easy_edit_view"/>
            <field name="arch" type="xml">

                <!-- Restrict standard_price label + field to group_product_cost -->
                <xpath expr="//label[@for='standard_price']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <xpath expr="//field[@name='standard_price']" position="attributes">
                    <attribute name="groups">stock_cost.group_product_cost</attribute>
                </xpath>
                <xpath expr="//field[@name='standard_price']" position="after">
                    <field name="foreign_standard_price"
                           widget='monetary' options="{'currency_field': 'force_currency_id', 'field_digits': True}"
                           class="float-left"/>
                </xpath>


                <xpath expr="//field[@name='currency_id']" position="after">
                                                                <field name="force_currency_id"/>
                </xpath>

            </field>
        </record>


    </data>
</odoo>
