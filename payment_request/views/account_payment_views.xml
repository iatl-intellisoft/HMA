<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>

        <record id="view_account_payment_request_form" model="ir.ui.view">
            <field name="name">account.payment.custody.form</field>
            <field name="model">account.payment</field>
            <field name="priority" eval="100"/>
            <field name="arch" type="xml">
                <form string="Register Custody Payment">
                    <sheet>
                        <group>
                            <field name="payment_type" invisible="1"/>
                            <field name="partner_type" invisible="1"/>
                            <field name="payment_request_id" invisible="1"/>
                            <field name="custody_clearance_id" invisible="1"/>
                            <field name="state" invisible="1"/>
                            <field name="available_partner_bank_ids" invisible="1"/>
                            <group>
                                <label for="amount"/>
                                <div name="amount_div" class="o_row">
                                    <field name="amount" readonly="1"/>

                                    <field name="currency_id" options="{'no_create': True, 'no_open': True}"
                                           groups="base.group_multi_currency"/>
                                </div>
                                <field name="partner_id" readonly="1" invisible="0"/>
                                <field name="company_id" invisible="1"/>

                                <field name="journal_id" widget="selection" domain="[('type', 'in', ('bank', 'cash'))]"
                                       readonly="0"/>

<!--                                <field name="hide_payment_method" invisible="1"/>-->
<!--                                <field name="suitable_journal_ids" invisible="1"/>-->
<!--                                <field name="available_payment_method_ids" invisible="1"/>-->
<!--                                <field name="require_partner_bank_account" invisible="1"/>-->

                                <field name="payment_method_id" widget="radio"
                                     />
    <field name="partner_bank_id" context="{'default_partner_id': partner_id}"
                                       invisible="show_partner_bank_account == False"
                                       readonly="state != 'draft'"/>

                                <!--                                <field name="payment_method_id" widget="radio" attrs="{'invisible': ['|', ('hide_payment_method', '=', True), ('amount', '=', 0.0)]}" />-->
                                <!--                                <field name="partner_bank_id" attrs="{'invisible': [('show_partner_bank_account', '!=', True)], 'required': [('show_partner_bank_account', '=', True)], 'readonly': [('state', '!=', 'draft')]}" context="{'default_partner_id': partner_id}" />-->
                                <field name="show_partner_bank_account" invisible="1"/>
                                <field name="payment_method_code" invisible="1"/>
                            </group>
                            <group>
                                <field name="date"/>
                                <field name="memo"/>
                                <field name="is_need_clearance" invisible="1"/>
                                <field name="destination_account_id"
                                       invisible="is_need_clearance != True"/>

                            </group>

                        </group>
                    </sheet>
                    <footer>
                        <button string="Validate" name="action_validate_custody_payment" type="object"
                                class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>

                </form>
            </field>
        </record>

        <record id="act_res_partner_2_custody" model="ir.actions.act_window">
            <field name="name">Custody Payments</field>
            <field name="res_model">account.payment</field>
            <field name="view_mode">list,form,graph</field>
            <field name="context">{'search_default_payment_request_id': active_id,'create':False}</field>
            <field name="domain">[('payment_request_id','=',active_id)]</field>

            <field name="groups_id" eval="[(4, ref('account.group_account_user'))]"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new quotation, the first step of a new sale!
                </p>
                <p>
                    Once the quotation is confirmed by the customer, it becomes a sales order.
                    <br/>
                    You will be able to create an invoice and collect the payment.
                </p>
            </field>
        </record>

        <record id="act_res_partner_3_custody" model="ir.actions.act_window">
            <field name="name">Custody Payments</field>
            <field name="res_model">account.payment</field>
            <field name="view_mode">list,form,graph</field>
            <field name="context">{'search_default_custody_clearance_id': active_id,'create':False}</field>
            <field name="domain">[('custody_clearance_id','=',active_id)]</field>

            <field name="groups_id" eval="[(4, ref('account.group_account_user'))]"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new quotation, the first step of a new sale!
                </p>
                <p>
                    Once the quotation is confirmed by the customer, it becomes a sales order.
                    <br/>
                    You will be able to create an invoice and collect the payment.
                </p>
            </field>
        </record>
    </data>
</odoo>
